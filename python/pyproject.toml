[project]
name = "mall"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ollama>=0.3.3",
    "polars>=1.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
